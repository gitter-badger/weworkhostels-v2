### Description
Job board for hostels and backpackers.

### Getting Started
* `npm install`
* `npm start`
* Navigate to http://localhost:1337

### Development
* `npm install`
* `npm install -g nodemon`
* `webpack -w`
* `nodemon server.js`
* Navigate to http://localhost:1337